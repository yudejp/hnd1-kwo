apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hnd1-meilisearch
  namespace: default
spec:
  defaultBackend:
    service:
      name: meilisearch
      port:
        number: 7700
  ingressClassName: tailscale
  tls:
    - hosts:
        - hnd1-meilisearch
